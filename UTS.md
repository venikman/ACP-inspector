| Row | Unified Tech             | Unified Plain                | FPF description (keywords)               | ACP 0.x Sense (term + gloss)                                     | JSON‑RPC 2.0 Sense                      | Project Sense (term + gloss)                                          | Bridges / CL + Rationale (brief)                                                                                     |
| --- | ------------------------ | ---------------------------- | ---------------------------------------- | ---------------------------------------------------------------- | --------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| R1  | ACPAgentParty            | Coding agent subprocess      | U.Holon; AgentRole; codebase scope       | Agent – generative coding subprocess                             | Server – request/response handler       | AgentUnderTest – instance under checks                                | Agent–Server ≈ CL2; Agent–AgentUnderTest ≡ CL3; party executes ACP methods                                           |
| R2  | ACPClientParty           | Editor or UI host            | U.Holon; ClientRole; issues requests     | Client – UI mediating between user and agent                     | Client – JSON‑RPC requester             | ClientEmulator – harness mimicking editor                             | Client–Client ≡ CL3; Client–ClientEmulator ≈ CL2 (no UI); controls agent & resources                                 |
| R3  | ACPSession               | Conversation session         | U.System; stateful; WorkScope over repo  | session – created via `session/new` or `session/load`            | *None* – no session concept             | SessionModel – persisted protocol state                               | session–SessionModel ≈ CL3; no JSON‑RPC peer; stateful channel of ACP turns                                          |
| R4  | PromptTurn               | Single prompt cycle          | U.Work; method `session/prompt`          | Prompt Turn – prompt → update stream → result                    | Request/Response – one call + result    | TurnExecution – behavioural validation unit                           | PromptTurn→Req/Resp ⊑ CL2; PromptTurn–TurnExecution ≡ CL3; minimal unit of ACP work                                  |
| R5  | ACPMethodCall            | Single ACP call              | U.Work; MethodDescription; schema based  | Method (e.g. `initialize`, `session/prompt`)                     | Method – named JSON‑RPC operation       | MethodProbe – wrapper emitting traces & checks                        | Method–Method ≡ CL3; Method–MethodProbe ≈ CL2 (probe adds side effects)                                              |
| R6  | ACPNotification          | One‑way ACP message          | U.Work; no result; logged evidence       | Notification (e.g. `session/update`)                             | Notification – no response              | EventCapture – listener asserting order & shape                       | Notification–Notification ≡ CL3; Notification–EventCapture ≈ CL2; fire‑and‑forget                                    |
| R7  | CapabilityFlag           | Declared feature flag        | U.Status; gates methods & WorkScope      | capability (e.g. `fs.readTextFile`) advertised in `initialize`   | *None*                                  | CapabilityMatrixEntry – spec row for each capability                  | capability–CapabilityMatrixEntry ≈ CL3; no JSON‑RPC peer; feature flag constraining behaviour                        |
| R8  | JsonRpcErrorObject       | JSON‑RPC error object        | U.Episteme; failure payload              | Error – ACP schema with code/message/data                        | error object – JSON‑RPC error structure | WireErrorRecord – trace record with id & direction                    | Error–error ≡ CL3; Error–WireErrorRecord ⊂ CL3 (adds indexing); structured failure payload                           |
| R9  | ProtocolErrorCode        | Error code classification    | U.Status; enumerated failure reason      | ErrorCode – standard + ACP‑specific codes (auth_required, etc.)  | error code – standard numeric codes     | ErrorClass – categories (TransportError, ProtocolViolation…)          | ErrorCode–error code ≡ CL3; ErrorCode–ErrorClass ≈ CL2 (grouped); canonical failure cause                            |
| R10 | CancelledTurnOutcome     | Cancelled prompt turn        | U.Status; non‑error cancellation         | StopReason.cancelled – must return on user cancel                | *None*                                  | CancellationVerdict – verdict of proper cancellation                  | StopReason–CancellationVerdict ≡ CL3; no JSON‑RPC peer; user‑driven early stop                                       |
| R11 | ACPDomainErrorOutcome    | Domain‑level error outcome   | U.Status; semantically expected failure  | auth_required, resource_not_found – ACP‑specific codes           | Server error (reserved range)           | DomainErrorVerdict – expected error correctly encoded                 | ACP code ⊂ JSON‑RPC server error CL2; DomainOutcome–Verdict ≡ CL3; user‑facing failure                               |
| R12 | ValidationFailure        | Validator‑detected violation | U.Episteme + U.Status; external verdict  | *None* – not defined by ACP                                      | *None*                                  | SpecViolation – kind ∈ {ProtocolInvariantBreach, SafetyPolicyBreach…} | External only (GroundingHolon); validation result; extra‑protocol diagnosis                                          |
| R13 | FileSystemResourceAccess | File read/write effect       | U.Work; Resource; captures intent & diff | File System – `fs/read_text_file`, `fs/write_text_file`          | *None*                                  | FileEffectObservation – record of path, op, before/after              | File System–FileEffectObservation ≈ CL3; no JSON‑RPC peer; observable file side effects                              |
| R14 | TerminalExecutionChannel | Terminal session             | U.System; TerminalRole; command channel  | Terminals – `terminal/create`, `terminal/kill`, embedded content | *None*                                  | TerminalProbe – probes commands, output & lifecycle                   | Terminal–TerminalProbe ≈ CL2 (observer); no JSON‑RPC peer; long‑lived command channel                                |
| R15 | ToolCallExecution        | Single tool call             | U.Work; lifecycle pending→completed      | Tool call – ACP tool statuses & content blocks                   | *None*                                  | ToolCallTrace – per‑call trace with outputs and effects               | ToolCallExecution–ToolCallTrace ≡ CL3; no JSON‑RPC peer; unit of tool work with effects                              |
| R16 | SessionTrace             | Full session trace           | U.Episteme; ordered events for checking  | session – prompt turns, updates, calls & cancellations (by id)   | JSON‑RPC trace – ordered messages       | ValidationTrace – canonical log keyed by sessionId                    | SessionTrace–JSON‑RPC trace ≈ CL2 (adds semantics); SessionTrace–ValidationTrace ≡ CL3; event history for invariants |
| R17 | MetaEnvelope             | Opaque metadata carrier      | U.Episteme; optional enrichment          | `_meta` – opaque map in ACP payloads                             | *None*                                  | MetaPassthrough – preserved, not validated unless rule opts in       | `_meta` → MetaPassthrough ≡ CL3; validation ignores unless profile enables                                          |
| R18 | ValidationFinding        | Structured validation event  | U.Episteme + U.Status; assurance signal  | *None* – ACP does not define                                    | *None*                                  | ValidationFinding – {ruleId,severity,message,context}                | External to ACP; bridges sentinel evidence into assurance lanes                                                          |
| R19 | StdioFramingDefault      | Transport framing defaults   | U.System; channel assumptions            | *None* – ACP transport agnostic                                 | UTF‑8 + LF framing; bounded message size | TransportAssumption – defaults for stdio runtime                     | Needed for interoperable runtime; guardrails for encode/decode and timeouts                                            |
